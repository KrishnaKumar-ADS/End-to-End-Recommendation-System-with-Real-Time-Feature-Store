# Core ML
torch==2.1.0+cpu  # CPU version for Docker (smaller, no CUDA driver needed)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Models
lightgbm>=4.0.0
faiss-cpu>=1.7.4

# Feature store
feast[redis]>=0.33.0
pyarrow>=13.0.0

# MLflow
mlflow>=2.8.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
httpx>=0.25.0

# Redis
redis>=5.0.0

# Utilities
psutil>=5.9.0
python-dotenv>=1.0.0
```

**Note:** On your local machine (RTX 3050), use `torch` without `+cpu` to keep GPU support. The `requirements.txt` above uses CPU torch for Docker to minimize image size. Create a separate `requirements-dev.txt` for local development:

**Create file:** `requirements-dev.txt`

```
# Same as requirements.txt but with GPU torch for local dev
torch>=2.1.0  # auto-detects CUDA
torchvision>=0.16.0
-r requirements.txt